{% extends 'base.html.twig' %}

{% block title %}Parking{% endblock %}

{% block body %}
    <h1>Parking</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parking.id }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ parking.matricule }}</td>
            </tr>
            <tr>
                <th>AllowedSize</th>
                <td>{{ parking.allowedSize }}</td>
            </tr>
            <tr>
                <th>Slots</th>
                <td>{{ parking.slots }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('parking_index') }}">back to list</a>

    <a href="{{ path('parking_edit', {'id': parking.id}) }}">edit</a>

    {{ include('parking/_delete_form.html.twig') }}
{% endblock %}