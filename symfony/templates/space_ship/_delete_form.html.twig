<form method="post" action="{{ path('space_ship_delete', {'id': space_ship.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ space_ship.id) }}">
    <button class="btn">Delete</button>
</form>