{% extends 'base.html.twig' %}

{% block title %}Edit SpaceShip{% endblock %}

{% block body %}
    <h1>Edit SpaceShip</h1>

    {{ include('space_ship/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('space_ship_index') }}">back to list</a>

    {{ include('space_ship/_delete_form.html.twig') }}
{% endblock %}